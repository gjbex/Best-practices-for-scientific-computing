# Temperature analysis

This small program computes the mean of temperature measurements and converts
the result from degrees Celsius to kelvin.

Run the analysis:

```bash
python3 temperature_analysis.py measurements.csv
```

Check the result against the known value:

```bash
python3 check_result.py
```

The example uses only the Python standard library.
